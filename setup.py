from setuptools import setup

setup(name='volatile_dictionary',
      version='0.1',
      description='Extension of the python native dictionary'
                  ' class that accept volatile sets.',
      url='',
      author='gyrao',
      author_email='pedrogyrao@hotmail.com',
      license='MIT',
      packages=['volatile_dictionary'],
      install_requires=['apscheduler'],
      zip_safe=False)
